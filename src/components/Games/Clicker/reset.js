export const reset = (setPosition, setClickListener1, setClickListener2,setScore1, setScore2) => {
    setPosition(0)
    setClickListener1(0)
    setClickListener2(0)
    setScore1(0)
    setScore2(0)
}
